AMZ = {
    # kmpmp due to merged cultures, we need to use the vanilla states instead of the culture
    # use_culture_states = yes

    states = {
        STATE_MATO_GROSSO
        STATE_AMAZONAS
        STATE_GUAYANA
        STATE_BOLIVAR
        STATE_GUAVIARE
        STATE_PASTAZA
        STATE_ACRE
        STATE_LA_PAZ
    }

    ai_will_do = { always = no }
}

CHE = {
    # kmpmp due to merged cultures, we need to use the vanilla states instead of the culture
    # use_culture_states = yes

    states = {
        STATE_ARKANSAS
        STATE_OKLAHOMA
    }

    ai_will_do = { always = no }
}

EGY = {
    # kmpmp due to merged cultures, we need to use the vanilla states instead of the culture
    # use_culture_states = yes

    states = {
        STATE_SINAI
        STATE_EGYPTIAN_DESERT
        STATE_MATRUH
        STATE_MIDDLE_EGYPT
        STATE_LOWER_EGYPT
        STATE_UPPER_EGYPT
    }

    required_num_states = 3

    ai_will_do = { always = no }
}

OCC = {
    # kmpmp - remove Rhone from releasable occitania
	# use_culture_states = yes

    states = {
        STATE_PROVENCE
        STATE_LANGUEDOC
        STATE_GUYENNE
        STATE_AUVERGNE_LIMOUSIN
        STATE_AQUITAINE
    }

	ai_will_do = { always = no }
}